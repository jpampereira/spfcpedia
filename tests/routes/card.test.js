// const request = require('supertest');

// const app = require('../../src/app');
// const { run } = require('../seed');

// const MAIN_ROUTE = '/card';

// beforeAll(() => {
//   run('08_goal_card');
// });

test('Deve listar todos os cartões', () => {});

test('Deve retornar um cartão por Id', () => {});

test('Deve inserir novos cartões com sucesso', () => {});

describe('Não deve inserir um cartão...', () => {});

describe('Deve atualizar um cartão com sucesso', () => {});

describe('Não deve atualizar um cartão...', () => {});

describe('Deve remover um cartão com sucesso', () => {});

describe('Não deve remover um cartão...', () => {});
